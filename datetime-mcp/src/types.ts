//type with field for ay of week, dat of moneth, moneth, year, hour, minute, second, timezone
export type DateTime = {
  dayOfWeek: string;
  dayOfMonth: number;
  month: number;
  year: number;
  hour: number;
  minute: number;
  second: number;
};
